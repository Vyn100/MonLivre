document.addEventListener('DOMContentLoaded', function () {
    const form = document.getElementById('reservationForm');

    form.addEventListener('submit', function (event) {
        event.preventDefault();

        // Vous pouvez ajouter ici le code JavaScript pour traiter le formulaire,
        // par exemple, envoyer les données au serveur via une requête AJAX.
        // Si vous avez besoin d'aide pour cette partie, n'hésitez pas à demander.
    });
});
